# Server
PORT=6000

# PostgreSQL
# DB_HOST is only for local npm run — Docker uses service name "db"
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=change_me_to_a_strong_password
DB_NAME=abk_db

# Optional SQL query logging
DB_LOGGING=false

# Frontend URLs allowed by CORS (comma-separated)
CORS_ORIGIN=https://abou-khalil.vercel.app,http://localhost:3000

# Default superAdmin created by seed
SEED_ADMIN_USERNAME=hassan
SEED_ADMIN_PASSWORD=P@ssw0rd
SEED_ADMIN_PHONE=0000000000
